// import MapArea from '@typings/MapArea';
import LocationData from '@/app/types/data/LocationData';

interface BaseLocationImageProps {
    // area: MapArea;
    // id: number;
    // getLocationData: (area: MapArea, id: number) => LocationData;
    locationData: LocationData | null;
    // setLocationData: (value: LocationData | null) => void;
}

export default BaseLocationImageProps;
